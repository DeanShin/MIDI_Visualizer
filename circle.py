class Circle():
    def __init__(self, color, gro_spd, x, y, size):
        self.color = color
        self.gro_spd = gro_spd
        self.x = x
        self.y = y
        self.size = size

    def update(self, pygame, window):


    def draw(self, pygame, window):
        pygame.draw.circle()